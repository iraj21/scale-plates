# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/

# Generated outputs (contain real restaurant data — never push)
output/

# Excel artifacts are generated by tools/make_template.py (sample data only),
# but we exclude binaries from the repo to avoid ever leaking client data
tools/*.xlsx

# Streamlit / local
.streamlit/
*.log

# OS
.DS_Store
Thumbs.db
